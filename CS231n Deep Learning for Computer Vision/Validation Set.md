Never use test set for tweaking

Spilt the data into train, validation, and test

> Split your training set into training set and a validation set. Use validation set to tune all hyperparameters. At the end run a single time on the test set and report performance.

Cross-validation: iterating over different validation sets and averaging the performance across these

![[Pasted image 20250925173001.png]]
